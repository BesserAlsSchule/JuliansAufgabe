#include "main.h"
#include <iostream>
#include <stdlib.h>
#include <cstring>

/// <summary>
/// fülle array values mit wert an index[0]
/// </summary>
/// <param name="values"></param>
/// <returns></returns>

int* c_neuenBehaelterAnErsterStelleEinbauen(int *values)
{
    //int uservalue = 0;
    //const int numberofValues = 4;
    //int numberOfValues = values[0];
    //int *Values = new int[numberOfValues];

    ////values = new int[sizeOfNewValues];

    //cout << "Das hier ist das aktuelle Array mit seinen Werten.\n";
    //for (int i = 1; i <= values[0]; i++)
    //{
    //    cout << values[i] << endl;
    //}

    //cout << "Welche Ganzzahl soll sich vor diesen Werten befinden?\n";
    //cin >> uservalue;

    //values[1] = uservalue;

    //for (int i = 2; i < values[0]; i++)   //Kopiere vom alten ins neue Array
    //{
    //    values[i] = values[i-1];
    //}

    //delete values;

    //cout << "Hier die neue Reihenfolge:\n";
    //for (int i = 1; i <= values[0]; i++)
    //{
    //    cout << values[i] << endl;
    //}
    //int* Values = (int*)malloc(sizeof(int[numberofValues]));

    //values = (int*)realloc(values, sizeof(int[numberofValues]));

    return values;   //verändert
}