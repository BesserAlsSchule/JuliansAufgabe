#include "main.h"
#include <iostream>
#include <stdlib.h>
#include <cstring>
#include <stdexcept>

using namespace std;

/// <summary>
/// ändere reihenfolge beliebiger werte im array
/// </summary>
/// <param name="values"></param>
/// <returns></returns>

int *l_beliebigenWertInZusammenstellungAendern(int *values)
{
    //int numberOfValues = values[0];
    //int *values = new int[numberOfValues];
    //int choice = 0;
    //int value = 0;
    //bool error = false;

    //cout << "Dies sind die aktuellen Werte:\n";

    //for (int i = 1; i < numberOfValues; i++)
    //{
    //    cout << values[i] << endl;
    //}

    //do  {
    //        cout << "Der wievielte dieser Werte soll ver\204ndert werden?\n";
    //        cin >> choice;
    //        if (choice < 0 || choice > numberOfValues)
    //        {
    //            cout << "Deine Eingabe befindet sich au\341erhalb des G\201ltigkeitsbereichs. Versuche es mit einem Wert zwischen  1 und " << numberOfValues << ".\n";
    //        }
    //    }   while (choice < 0 || choice > numberOfValues);

    //do {
    //    try {
    //            cout << "Welche Ganzzahl soll eingesetzt werden?\n";
    //            cin >> value;
    //            error = false;
    //        }
    //        catch(out_of_range &e)
    //        {
    //            cout << "Es ist ein Fehler aufgetreten.\tVersuchen wir es erneut.\n";
    //            error = true;
    //        }
    //    }   while(error != false);

    //values[choice] = value;

    //cout << "Dies sind die aktuellen Werte:\n";

    //for (int i = 1; i <= numberOfValues; i++)
    //{
    //    cout << values[i] << endl;
    //}

    //return values;  //verändert
    return;
}

